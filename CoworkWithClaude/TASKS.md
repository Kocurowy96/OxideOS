# OxideOS — Lista Zadań

Wspólna lista zadań dla trybu pracy: w tygodniu (np. gdy Kocurowy96 jest w szkole) Claude
może samodzielnie lecieć z pozycjami stąd, a wieczorem/w weekend wspólnie testujemy
i rozbudowujemy tę listę o nowe pomysły.

## Jak z tego korzystać
- **Do zrobienia teraz** — bezpieczne do zrobienia bez nadzoru: dobrze opisane, wąskie zadania.
- **Do przegadania** — wymaga decyzji/doprecyzowania z Kocurowy96 zanim ruszy autonomicznie.
- **Zrobione** — przenosić tu po zamknięciu, z datą i krótką notką (przyda się do historii).

## Automatyzacja (od 2026-09-11)
W dni szkolne (pon-pt, 9:00 czasu polskiego) w chmurze odpala się agent Claude, który:
1. czyta ten plik, bierze pierwszą niezajętą pozycję z "Do zrobienia teraz",
2. robi jeden sensowny kawałek (buduje + testuje headless w QEMU),
3. aktualizuje ten plik (przenosi do "Zrobione" albo dopisuje notkę o postępie),
4. commituje i pushuje **tylko na gałąź `daily-work`** — main zostaje nietknięty.

Wieczorem: `git fetch && git checkout daily-work` żeby zobaczyć/przetestować co się zmieniło.
Panel: https://claude.ai/code/routines/trig_01WyfN223HwBRWFr65d18VKx

Agent po każdym uruchomieniu dopisuje też lekki raport do `daily-work-report/` (patrz
[README](../daily-work-report/README.md) i [szablon](../daily-work-report/TEMPLATE.md)) —
szybszy podgląd co zrobił niż grzebanie w logach sesji w chmurze.

**2026-09-11 wieczorem: zrobiony i zweryfikowany pierwszy pełny cykl testowy** (ręcznie
odpalony, dwie próby). Pierwsza utknęła na `rm -rf limine_dir/*` (monit bezpieczeństwa bez
nikogo do zatwierdzenia - poprawione w promptcie, patrz `HOW_WE_WORK.md`). Druga przeszła
cały cykl: zakładka Mysz zaimplementowana, zbudowana, przetestowana headless w QEMU,
zacommitowana i wypchnięta na świeżo utworzoną gałąź `daily-work` (commit `04d26f7`).
Do przetestowania w prawdziwym GUI.

---

## Do zrobienia teraz

- [ ] **Ext2 Faza 2a** (patrz `PLAN_ext2_filesystem.md`): bitmapa wolnych bloków — funkcje do alokacji/zwalniania bloku danych w `kernel/fs/ext2.cpp`, operujące na `group_desc_table[grupa].block_bitmap` (bitmapa bit-per-blok, 1 = zajęty, tak jak zwykły ext2). Na start wystarczy grupa 0 (obrazy testowe `mke2fs` na małych dyskach mają zwykle jedną grupę — potwierdzić przez `dumpe2fs`/log z `Init()`, `block_groups_count`). Potrzebne dwie funkcje prywatne (analogicznie do `ReadInode`/`ResolvePath`): `AllocateBlock()` (szuka pierwszego zerowego bitu w bitmapie, ustawia go na 1, zmniejsza `free_blocks_count` w grupie i w superbloku, zwraca numer bloku) i `FreeBlock(uint32_t block_num)` (odwrotnie). Do testu: tymczasowy kod w `Init()` (wycofać przed commitem) który alokuje kilka bloków pod rząd i loguje numery, potem `e2fsck -f` na obrazie hosta powinien nie zgłosić błędów bitmapy jeśli zapis na dysk faktycznie działa (uwaga: to pierwszy *zapis* sterownika ext2 na dysk — dziś wszystko było tylko odczytem, więc to dobra okazja żeby też ręcznie zweryfikować przez `debugfs -R "stat <2>"`/porównanie bitmapy bajt-po-bajcie z `dd`+hexdump przed/po, nie tylko ufać `e2fsck`).

## Do przegadania

- [ ] `libgui` (`gui_draw_rect`/`gui_draw_string`) nie sprawdza granic w Y w ogóle (i `gui_draw_string` w ogóle nie sprawdza X) — znalezione 2026-09-20 przy naprawianiu Notatnika. Dziś każda apka musi sama pilnować żeby nie rysować poza `win_h`/`win_w` (Notatnik ma teraz własny scroll-clamp jako obejście), ale to systemowa dziura — bez per-process page tables przekroczenie granicy okna to zapis w cudzą pamięć (innego okna albo jądra), nie tylko wizualny glicz. Wymaga dodania `win_h` do sygnatur obu funkcji w `apps/libgui/gui.h`/`gui.c` i przejścia przez wszystkie call site'y w każdej apce — szerszy zasięg zmiany, do przegadania.
- [ ] Redesign assetów — wbudowanie kluczowych plików (kursor, ikony) w binarkę kernela zamiast ładowania z FAT32 w runtime (patrz `podsumowanie_projektu.md` / decyzja z 2026-09-10)
- [ ] Ext2 Fazy 2a-3 (bitmapy alokacji, zapis, potem integracja z VFS i przełączenie `disk.img`) — pełny rozpisany plan w `PLAN_ext2_filesystem.md`, dopisywać do "Do zrobienia teraz" krok po kroku w miarę postępu (Faza 1a zrobiona 2026-09-21, 1b 2026-09-22, 1c 2026-09-23, 1d 2026-09-24, 1e 2026-09-25, 1f 2026-09-26 — cały odczyt gotowy, Faza 2a (bitmapa bloków) w "Do zrobienia teraz"), nie hurtowo.
- [ ] Menu Start: rekursywne submenu dla podfolderów w `/usr/bin` (dziś płaska lista — do rozszerzenia jak pojawi się realny podfolder do przetestowania)
- [ ] Skompilować i odpalić SerenityOS lokalnie (mamy klon w `.serenity/`) — rekonesans, jak wygląda dojrzały hobby-OS i jak Ladybird tam faktycznie działa
- [ ] Sieć / TCP-IP stack — zero na razie w OxideOS; wymagane zanim jakikolwiek browser (nawet coś dużo mniejszego niż Ladybird) miałby sens
- [ ] Realny model procesów z osobnymi tabelami stron per proces (dziś: jedna wspólna przestrzeń adresowa dla wszystkich tasków — zdiagnozowane 2026-09-11 przy okazji buga ze zniszczonymi oknami; per-task kernel stack już naprawiony, ale to kolejny krok w tym samym kierunku)
- [ ] "Port debugowy" — pozostał tylko etap 4 (1, 2, 3 zrobione 2026-09-20, patrz "Zrobione" i `PLAN_scripty_i_debug.md`): nagrywanie ekranu — TYLKO lokalnie na maszynie roboczej (Garuda Linux/KWin/Wayland, ASUS TUF Gaming A15), nie w chmurze. Najmniej pilne, opcjonalne.

## Zrobione

- [x] (2026-09-26) **Ext2 Faza 1f** (patrz `PLAN_ext2_filesystem.md`): rozszerzenie `Ext2::ReadFile` w `kernel/fs/ext2.cpp` o obsługę bloku pojedynczo pośredniego (`inode.block[12]`) dla plików > 12×block_size. Odrzucenie plików wymagających bloku pośredniego zamienione na: policzenie `pointers_per_block = block_size / sizeof(uint32_t)`, odrzucenie tylko gdy `blocks_needed > 12 + pointers_per_block` (podwójnie/potrójnie pośrednie, `block[13]`/`block[14]`, wciąż nieobsługiwane — log "not supported yet"). Dla indeksu bloku `i >= 12` blok pośredni (`inode.block[12]`) doczytywany raz do bufora na stosie (`indirect_block[EXT2_MAX_BLOCK_SIZE/4]`, analogicznie do `block_buf` w `ResolvePath`/`ListDirectory`) i numer bloku danych brany jako `indirect_block[i-12]`; dziura (`block[12] == 0` albo wpis w tablicy pośredniej równy 0) dalej odrzucana jako nieobsługiwany sparse file, tak jak w Fazie 1e dla bloków bezpośrednich. Reszta funkcji (alokacja bufora przez PMM, przycinanie ostatniego bloku do `size - already_read`, `FreeFile`) bez zmian względem Fazy 1e. `kernel/fs/ext2.h`: komentarz nagłówkowy zaktualizowany (Faza 1f gotowa). Środowisko chmurowe tej sesji (świeży kontener) nie miało `qemu-system-x86_64`/`mtools`/`xorriso`/`nasm`/`imagemagick`/`e2fsprogs` — doinstalowane przez `apt-get`; `limine_dir` zbudowany przez `./configure --prefix=.../limine_dir --enable-bios --enable-bios-cd --enable-uefi-x86-64 --enable-uefi-cd && make && make install` (wcześniejsze sesje najwyraźniej używały innego zestawu flag/kroków — bez `--enable-*` `make` budował tylko `bin/limine`, host tool, nie same bootloadery; ISO/dysk mimo to poszły bez problemu po dograniu brakujących binarek do `iso_root/boot/limine/` + ręcznie złożonym `limine.conf`); `imagemagick` znów tylko IM6 (`convert`, nie `magick`) — ten sam znany obejście, symlink `/usr/local/bin/magick -> /usr/bin/convert`.
  Zweryfikowane na obrazie testowym (`mke2fs -t ext2 -b 4096`, przez `debugfs -w`): (1) regresja — `hello.txt` (22B) i `bigfile.txt` (10000B, 3 bloki bezpośrednie) z Fazy 1e dalej czytają się poprawnie; (2) plik dokładnie na granicy 12 bloków bezpośrednich (`exact12.txt`, 49152B = 12×4096, bez bloku pośredniego) — 49152 bajty, treść zgodna bajt-po-bajcie z deterministycznym wzorcem źródłowym (`i%10` jako cyfra ASCII) na całej długości; (3) plik o jeden bajt większy (`exact13.txt`, 49153B — pierwszy bajt wymagający bloku pośredniego) — też w pełni zgodny, potwierdza poprawne przejście przez granicę 12/13; (4) większy plik na wielu blokach pośrednich (`bigfile2.txt`, 200000B = 49 bloków, 12 bezpośrednich + 37 przez pojedynczo pośredni) — zweryfikowany podwójnie: w pętli w kernelu (dopisanej tymczasowo do `Init()` na czas testu i wycofanej przed commitem, jak w poprzednich fazach) bajt-po-bajcie względem wzorca źródłowego — zgodność 100%, ORAZ przez `cmp` na hoście między plikiem źródłowym a `debugfs -R "cat bigfile2.txt" test.img` — identyczne. `debugfs -R "stat bigfile2.txt"` na hoście potwierdza układ bloków `(0-11):270-281, (IND):282, (12-48):283-319` — zgodnie z oczekiwaniem (13 bloków metadanych+danych: 12 bezpośrednich + 1 blok pośredni wskazujący na kolejne 37).
  Pełny czysty rebuild PASS (zero nowych warningów, te same 9 pre-existing co przed zmianą). Headless boot BEZ `-hdb` (konfiguracja produkcyjna) PASS bez regresji (`Ext2: Failed to read superblock.` — oczekiwany ATA timeout, brak drugiego dysku, `ReadFile` w ogóle nie wołane). Headless boot Z `-hdb` (obraz testowy z wszystkimi plikami wyżej) PASS, `ListDirectory(/)` pokazuje wszystkie 6 wpisów z poprawnymi rozmiarami, `ReadFile(/hello.txt)` (permanentny test w `Init()`) dalej działa. Faza 2a (bitmapa wolnych bloków, pierwszy krok zapisu) dopisana do "Do zrobienia teraz" — świadomie NIE podjęta w tej samej sesji mimo dostępnego czasu/kontekstu: to pierwszy krok który wprowadza faktyczny zapis na dysk (dziś sterownik ext2 jest czysto odczytowy), więc zasługuje na osobną, w pełni skupioną sesję weryfikacyjną zamiast doklejenia na końcu tej — zgodnie z ustaloną preferencją małych, przeglądalnych kroków zamiast maksymalizowania postępu w jednej sesji.
- [x] (2026-09-25) **Ext2 Faza 1e** (patrz `PLAN_ext2_filesystem.md`): `Ext2::ReadFile(path, out_buffer, out_size)` w `kernel/fs/ext2.cpp` dla plików mieszczących się w 12 blokach bezpośrednich (`inode.block[0..11]`). Rozwiązuje `path` przez `ResolvePath` (Faza 1c), czyta i-węzeł, sprawdza `S_IFREG` (jeśli nie plik, zwraca `false` — analogicznie do sprawdzenia `S_IFDIR` w `ListDirectory`), odrzuca pliki wymagające bloku pośredniego (`blocks_needed > 12`, log "Faza 1f, not supported yet") i dziury w pliku (`block[i] == 0`, sparse — też nieobsługiwane). Bufor alokowany przez `PMM::AllocatePages`/`hhdm_request` (jak `group_desc_table` w `Init()`), rozmiar zaokrąglony w górę do stron; kolejne `block[0..11]` czytane przez `ReadDiskBytes`, przy czym każdy odczyt jest przycięty do `min(block_size, inode.size - already_read)` — ostatni (częściowo pełny) blok nigdy nie kopiuje bajtów za końcem pliku, zgodnie z opisem zadania. `Ext2::FreeFile` zaimplementowane analogicznie do `FAT32::FreeFile` (konwersja hhdm→fizyczny adres + `PMM::FreePages`). Zweryfikowane na obrazie testowym (`mke2fs -t ext2 -b 4096`, przez `debugfs -w`): mały plik `hello.txt` (22B, jeden blok) — `ReadFile` zwrócił dokładnie te same 22 bajty co zapisane źródło (widoczne w logu serialowym z `Init()`, gdzie na stałe zostaje test `ReadFile("/hello.txt", ...)`, bezpieczny na obrazach bez tego pliku — po prostu loguje "not found"). Większy plik `bigfile.txt` (10000B, 3 bloki po 4096, ostatni blok częściowo pełny) — zweryfikowany tymczasowym testem w `Init()` (wycofanym przed commitem, jak w poprzednich fazach): odczytane dokładnie 10000 bajtów, treść zgodna bajt-po-bajcie z deterministycznym wzorcem źródłowym na całej długości (w tym poprawne obcięcie ostatniego, niepełnego bloku), potwierdza poprawne przejście przez więcej niż jeden `block[]`. Pełny czysty rebuild PASS (zero nowych warningów). Headless boot BEZ `-hdb` (konfiguracja produkcyjna) PASS bez regresji (`Ext2: Failed to read superblock.` — oczekiwane, brak drugiego dysku, `ReadFile` w ogóle nie jest wtedy wołane bo `Init()` wraca wcześniej). Headless boot Z `-hdb` (obraz testowy) PASS z logami jak opisano wyżej. Faza 1f (odczyt przez pojedynczo pośredni blok, pliki >12×block_size) dopisana do "Do zrobienia teraz".
- [x] (2026-09-24) **Ext2 Faza 1d** (patrz `PLAN_ext2_filesystem.md`): `Ext2::ListDirectory(path, out_entries, max_entries)` w `kernel/fs/ext2.cpp` — pierwsza publiczna funkcja `Ext2::` z prawdziwą implementacją poza `Init()`. Rozwiązuje `path` przez `ResolvePath` (Faza 1c), sprawdza `S_IFDIR` (jeśli nie katalog, zwraca 0), czyta pierwszy blok przez `ReadDirectoryFirstBlock` (to samo uproszczenie "tylko `block[0]`" co w Fazie 1c) i przechodzi po wpisach `Ext2DirEntry` przeskakując o `rec_len` (jak `FindEntryInBlock`), pomijając `inode == 0` oraz `"."`/`".."` (analogicznie do `FAT32::ListDirectory` pomijającego wpisy zaczynające się od `.`, `fat32.cpp:639`). Wpis katalogowy ext2 ma tylko `inode`+`file_type`, więc `size`/`attributes` (`FS_ATTR_DIRECTORY`) wymagają dodatkowego `ReadInode` na każdym znalezionym wpisie. Zweryfikowane na obrazie testowym (`mke2fs -t ext2`, blok 4K, przez `debugfs -w` dodane `hello.txt`/17B, `bigfile.txt`/500B, pusty podkatalog `subdir`): `ListDirectory("/")` zwrócił 4 wpisy (`lost+found` DIR 16384, `hello.txt` FILE 17, `bigfile.txt` FILE 500, `subdir` DIR 4096) — **identyczne 1:1** z `debugfs -R "ls -l /" test.img` (te same nazwy/rozmiary/atrybuty, bez `.`/`..`). Dodatkowo (stretch, tymczasowo dopisane do `Init()` na czas testu, wycofane przed commitem) zweryfikowany pusty podkatalog: `ListDirectory("/subdir")` → 0 wpisów, zgodnie z oczekiwaniem (tylko `.`/`..`, oba odfiltrowane). Pełny czysty rebuild PASS (zero nowych warningów). Headless boot BEZ `-hdb` (konfiguracja produkcyjna) PASS bez regresji (ten sam ATA timeout jak w poprzednich fazach, FAT32/kompozytor/HELLO.ELF ładują się normalnie). Headless boot Z `-hdb` (obraz testowy) PASS z logami jak opisano wyżej. Faza 1e (`ReadFile` dla bloków bezpośrednich) dopisana do "Do zrobienia teraz".
- [x] (2026-09-23) **Ext2 Faza 1c** (patrz `PLAN_ext2_filesystem.md`): rozwiązywanie ścieżek w `kernel/fs/ext2.cpp`. Nowa struktura `Ext2DirEntry` (packed: `inode`/`rec_len`/`name_len`/`file_type`, nazwa BEZ terminatora `\0` zaraz po strukturze) + dwie statyczne pomocnicze funkcje: `ReadDirectoryFirstBlock` (czyta `block[0]` katalogu przez `ReadInode`, uproszczenie "tylko pierwszy blok bezpośredni" — zaznaczone komentarzem do rozszerzenia w 1e/1f) i `FindEntryInBlock` (liniowe przejście po wpisach bloku, przeskok o `rec_len`, porównanie `name_len` bajtów bez zakładania `\0`). Nowa statyczna `ResolvePath(path, out_inode_nr)`: dzieli ścieżkę po `/`, dla każdego segmentu sprawdza że bieżący i-węzeł jest katalogiem (`S_IFDIR`) i szuka w nim wpisu po nazwie, znaleziony `inode` staje się nowym bieżącym — analogicznie do `FindDirectoryCluster`/`ResolveDirectoryCluster` w `fat32.cpp`, tylko na i-węzłach zamiast klastrach FAT. Jeszcze NIE publiczne API `Ext2::` (prywatna w `ext2.cpp`, jak `ReadInode`) — skorzysta z niej dopiero Faza 1d (`ListDirectory`). Dodany też permanentny test w `Init()`: `ResolvePath("/lost+found")` — katalog który `mke2fs` tworzy domyślnie w każdym świeżym obrazie, więc test działa bez żadnego ręcznego przygotowania obrazu (w przeciwieństwie do dowolnej nazwy pliku testowego). Zweryfikowane na obrazie testowym (`mke2fs -t ext2`, blok 4K): `ResolvePath("/lost+found")` → i-węzeł 11, **identyczne** z `debugfs -R "stat lost+found"`. Dodatkowo (stretch, tymczasowo dopisane do `Init()` na czas testu, wycofane przed commitem): plik w roocie dodany przez `debugfs -w -R "write ... testfile.txt"` (przydzielony i-węzeł 12) → `ResolvePath("/testfile.txt")` zwrócił 12; katalog zagnieżdżony `debugfs -w -R "mkdir /sub"` (i-węzeł 13) + plik w środku `sub/nested.txt` (i-węzeł 14) → `ResolvePath("/sub")`=13, `ResolvePath("/sub/nested.txt")`=14 — wszystkie 1:1 zgodne z `debugfs stat`, potwierdza poprawność rekursji przez segmenty ścieżki, nie tylko przypadek jednopoziomowy. Pełny czysty rebuild PASS (zero nowych warningów). Headless boot BEZ `-hdb` (konfiguracja produkcyjna) PASS bez regresji (ten sam ATA timeout ~130ms jak w poprzednich fazach, FAT32/kompozytor/HELLO.ELF ładują się normalnie). Headless boot Z `-hdb` (obraz testowy) PASS z logami jak opisano wyżej. Faza 1d (`ListDirectory`) dopisana do "Do zrobienia teraz".
- [x] (2026-09-22) **Ext2 Faza 1b** (patrz `PLAN_ext2_filesystem.md`): odczyt tablicy i-węzłów w `kernel/fs/ext2.cpp` — nowa struktura `Ext2Inode` (packed, 128 bajtów, `mode`/`size`/`atime`/`ctime`/`mtime`/`dtime`/`gid`/`links_count`/`blocks`/`flags`/`block[15]`/`generation`/`file_acl`/`dir_acl`/`faddr`, zgodnie ze specyfikacją ext2 rev 0/1) i statyczna `ReadInode(inode_nr, out)`: grupa = `(inode_nr-1)/inodes_per_group`, indeks w grupie = `(inode_nr-1)%inodes_per_group`, offset = `group_desc_table[grupa].inode_table*block_size + indeks*inode_size` (`inode_size` brane wprost z pola `superblock.inode_size`, statyczny `superblock` z Fazy 1a już żyje poza `Init()`, więc nie trzeba było dodawać osobnej zmiennej modułu — obcina odczyt do `sizeof(Ext2Inode)` gdyby `inode_size` był większy, np. 256 przy nowszych `mke2fs`). `Init()` po zbudowaniu BGDT odczytuje i-węzeł root (numer 2) i loguje `mode`/rozpoznanie `S_IFDIR`/`size`/`links_count`/`blocks`/pierwsze dwa `block[]`. NIE podłączone do `VFS::` — FAT32/`disk.img` bez zmian. Zweryfikowane na tym samym typie obrazu testowego co Faza 1a (`mke2fs -t ext2 test.img`, `-hdb`): odczyt i-węzła root (`mode=0x41ed`→S_IFDIR, `size=4096`, `links_count=3`, `blocks=8`, `block[0]=260`) **identyczny 1:1** z `debugfs -R "stat <2>" test.img` na hoście. Dodatkowo (stretch z opisu zadania) zweryfikowany też wyższy numer i-węzła — plik dodany przez `debugfs -w -R "write ..." test.img` (przydzielony i-węzeł 12, nie sąsiadujący z rootem w tablicy) odczytany przez `ReadInode(12,...)` dał `mode=0x81a4`/`size=70`/`links=1`/`blocks=8`/`block[0]=266`, znów identyczne z `debugfs stat` — potwierdza poprawność ogólnego wzoru grupa/indeks, nie tylko przypadku granicznego root=2 (test tymczasowo dopisany do `Init()`, zweryfikowany w QEMU, potem wycofany przed commitem — nie zostaje w kodzie na stałe). Pełny czysty rebuild PASS, headless boot BEZ `-hdb` (konfiguracja produkcyjna) PASS bez regresji (ATA timeout ~jak w Fazie 1a, FAT32/kompozytor/HELLO.ELF ładują się normalnie), headless boot Z `-hdb` (obrazem testowym) PASS z poprawnymi logami jw. Faza 1c (rozwiązywanie ścieżek) dopisana do "Do zrobienia teraz".
- [x] (2026-09-21) **Ext2 Faza 1a** (patrz `PLAN_ext2_filesystem.md`): nowy `kernel/fs/ext2.h`/`ext2.cpp` (interfejs jak `fat32.h`, na razie tylko `Init()` zaimplementowany, `ReadFile`/`WriteFile`/`FreeFile`/`ListDirectory` same deklaracje). `Ext2::Init()` parsuje superblok (offset 1024, magic 0xEF53, `log_block_size`, `blocks_per_group`/`inodes_per_group` itd.) i tablicę deskryptorów grup bloków (alokowaną przez `PMM`, blok `first_data_block+1`), loguje `Ext2: Initialized successfully.` + pola diagnostyczne. NIE podłączone do `VFS::` — `disk.img`/`FAT32::` zostają nietknięte i aktywne jak dotąd. Do testu dodane `ATA::ReadSectorSlave`/`ReadSectorsSlave` (primary slave, np. QEMU `-hdb`) obok istniejącego `ATA::ReadSectors` (master) — wspólny `ReadSectorsInternal` z parametrem drive-select. Zweryfikowane: `mke2fs -t ext2 test.img` (dwa warianty — domyślny 4K blok i `-b 1024`, żeby przetestować obie gałęzie liczenia offsetu BGDT), podłączone jako `-hdb` w headless boot QEMU, log serialowy porównany pole-po-polu z `dumpe2fs test.img` na hoście — 100% zgodność (magic/block_size/blocks_count/inodes_count/blocks_per_group/inodes_per_group/first_data_block/block_groups_count oraz group[0] block_bitmap/inode_bitmap/inode_table/free_blocks/free_inodes). **Przy okazji znaleziony i naprawiony realny bug**: pierwsza wersja `ReadSectorsSlave` (bez ograniczenia pętli oczekiwania na DRQ) wieszała CAŁY rozruch w nieskończoność, gdy nie ma podłączonego drugiego dysku (czyli w normalnej, produkcyjnej konfiguracji z samym `-hda`!) — status rejestru dla nieobecnego slave'a nie zawsze wraca 0xFF jak dla całkiem odłączonej magistrali, więc pętla `while (!DRQ)` nigdy się nie kończyła. Naprawione dodaniem limitu iteracji (`ATA_MAX_POLL_SPINS`) do obu pętli oczekiwania (BSY i DRQ) w `ReadSectorsInternal` — zmierzony koszt przy braku drugiego dysku: ~130ms jednorazowo przy starcie, bez wpływu na dalszy rozruch. Pełny czysty rebuild + headless boot (z i bez `-hdb`) zweryfikowane po poprawce, `scripts/test_headless.sh` PASS. Kolejny krok (Faza 1b: odczyt tablicy i-węzłów) do dopisania osobno, nie teraz.
- [x] (2026-09-20) Klawiatura (`kernel/drivers/ps2_kbd.cpp`) obsługuje teraz Shift i Caps Lock — dwie tablice scancode→ascii (unshifted/shifted), stan `shift_held`/`caps_lock` śledzony przez key-up/key-down (wcześniej sterownik w ogóle nie obsługiwał zwolnienia klawisza), wielkość liter z `shift_held XOR caps_lock`, symbole (`!@#$%^&*()_+{}:"~|<>?` itd.) tylko z Shift. Dotyczy każdej apki z wpisywaniem tekstu, nie tylko Notatnika. Przy okazji rozszerzona mapa znaków w `scripts/qmp_input.py` (nasze narzędzie testowe) o pełen zestaw symboli z Shift — przetestowane end-to-end w Notatniku ("Hello World Test 123", "Hasl0! Test#2 [a-z]=OK" renderują się poprawnie).
- [x] (2026-09-20) Notatnik przestał być surowym canvasem: dodany syscall `sys_read_file` (5, analogiczny do `sys_write_file`, kopiuje zawartość pliku do bufora appki przez `VFS::ReadFile`+`VFS::FreeFile`), menu "Zapisz"/"Otworz"/"Nowy" realnie działa (na sztywno `/notatka.txt`, bez file-pickera na razie), status w rogu menu ("Zapisano."/"Wczytano."/"Brak pliku"). Dodany też auto-scroll trzymający kursor widocznym — przy okazji naprawia realny bug: bez tego długi tekst rysowałby się poza `win_h` (biblioteka `gui_draw_string`/`gui_draw_rect` w `libgui` nie sprawdza granic w Y w ogóle — zobacz notkę niżej w "Do przegadania", to dotyczy WSZYSTKICH apek, nie tylko Notatnika). Przetestowane end-to-end (zapis→nowy→otwórz przywraca treść, 40 linii tekstu poprawnie scrolluje bez wycieku poza okno) przez `headless_interact.sh` + zrzuty ekranu.
- [x] (2026-09-20) "Port debugowy" etap 2: `scripts/gdb_inspect.sh <plik_komend_gdb> [wait_s]` — boot headless z GDB stubem QEMU (`-s`, bez `-S` żeby złapać stan "w locie" a nie od resetu), wykonuje podane komendy GDB (np. `info registers`, `bt`, `x/16i $pc`) na żywym systemie i drukuje wynik + log serialowy. Kernel.elf nie ma DWARF, ale ma symbole (mangled C++) — `bt` pokazuje realne nazwy funkcji. Przetestowane — złapało system w środku `Framebuffer::Clear`←`Compositor::Render`←`DesktopTask`, czytelny wynik. Szczegóły: `PLAN_scripty_i_debug.md`.
- [x] (2026-09-20) Sami (nie agent) naprawiliśmy trzy drobne bugi które agent dwukrotnie zgłaszał: (1) brakujący `#include <stdbool.h>` w `apps/calculator/main.c`; (2) `HELLO.ELF` link fail pod nowszym binutils — `objcopy --remove-section=.note.gnu.property` na `apps/hello/main.o` przed linkowaniem w `scripts/build.sh` (lokalnie nie było problemu, więc nie dało się tego bezpośrednio zreprodukować, ale poprawka jest bezpieczna — sekcja niepotrzebna we freestanding ELF, usunięcie jej nie zmienia zachowania, tylko usuwa źródło konfliktu segmentów); (3) `scripts/make_disk.sh` wykrywa teraz czy binarka limine jest w `limine_dir/limine` czy `limine_dir/bin/limine` zamiast zakładać z góry. Zweryfikowane pełnym czystym rebuildem i headless bootem — PASS, oba `HELLO.ELF`/`CALC.ELF` budują się.
- [x] (2026-09-20) "Port debugowy" etap 3: `scripts/qmp_input.py` (klik/scroll/klawiatura przez QMP `input-send-event`, dzieli `qmp_client.py` z etapem 1) + `scripts/headless_interact.sh <scenariusz> [katalog] [boot_wait_s]` — czyta sekwencję akcji z pliku (move/click/scroll/key/type/wait/shot) i wykonuje je na headless QEMU. W trakcie wdrażania znaleziony i naprawiony realny bug w narzędziu: klik bez przytrzymania (down+up od razu) był czasem "niewidoczny" dla `mouse_clicked = mouse_left && !prev_mouse_left` w `compositor.cpp` — fix: 150ms przytrzymania. Po fixie potwierdzone działające na żywo: zamknięcie dialogu powitalnego, otwarcie Menu Start, uruchomienie apki z niego, wpisywanie tekstu. Szczegóły: `PLAN_scripty_i_debug.md`.
- [x] (2026-09-20) "Port debugowy" etap 1: `scripts/screendump.sh` [out.png] [wait_s] — boot headless, czeka `wait_s` (domyślnie 8s), robi zrzut ekranu emulatora przez QMP `screendump` (gniazdo unix, `scripts/qmp_screendump.py`), konwertuje PPM→PNG (magick/convert), drukuje log serialowy. Pozwala "zobaczyć" GUI bez człowieka przy ekranie — przetestowane, działa (zrzut z ekranem powitalnym HELLO.ELF). Etapy 2 i 4 (GDB stub, nagrywanie) zostają w "Do przegadania".
- [x] (2026-09-15) Rozbicie `scripts/run.sh` na `scripts/build.sh` (kernel+apki), `scripts/make_disk.sh` (dysk.img+ISO z gotowych binarek) i `scripts/test_headless.sh` (build+dysk+headless boot w QEMU, log do pliku, PASS/FAIL po `FAT32: Initialized successfully` + braku "kernel panic" — kryterium z `PLAN_scripty_i_debug.md`). `scripts/run.sh` woła teraz `build.sh` + `make_disk.sh` i tylko odpala QEMU z GUI/audio — logika 1:1 przeniesiona, bez zmian w treści komend. Zweryfikowane pełnym czystym rebuildem oraz headless bootem (patrz raport z tej sesji po szczegóły i napotkane pre-existing problemy: link HELLO.ELF, brak stdbool.h w CALC.ELF, niezgodność ścieżki `limine_dir/limine` vs `limine_dir/bin/limine`).
- [x] (2026-09-14) Sprzątnięcie write-only ikon w `compositor.cpp` — usunięte `icon_programy`, `icon_clock`, `icon_folder_32` (statyczne zmienne + odpowiadające im wczytania `VFS::ReadFile` w `Init()`), bo nigdzie już nie były rysowane po przejściu na dynamiczne Menu Start. Zweryfikowane pełnym czystym buildem i headless bootem w QEMU (logi pokazują trzy mniej odczytów z FAT32 przy starcie, bez regresji).
- [x] (2026-09-11) Ustawienia: zakładka **Mysz** (czułość kursora 25-300%, `-`/`+`, kolejność Wyświetlacz → System → Dźwięk → Mysz) — nowe syscalle 11/12 (`sys_set_mouse_speed`/`sys_get_mouse_speed`), mnożnik czułości w `Mouse::HandleInterrupt` (dotyczy tylko trybu relatywnego PS/2 — QEMU domyślnie używa VMMouse w trybie absolutnym, więc efekt nie jest widoczny pod QEMU, ale realny na sprzęcie/w trybie relatywnym)
- [x] (2026-09-10) LFN w FAT32, Start Menu (styl + ikony), optymalizacja I/O dysku, redesign Panelu Sterowania (kafelki, tapety z miniaturkami)
- [x] (2026-09-11) Fix kompilacji (`icon_speaker`), tray z ikoną głośnika, sprzątanie repo (`.ELF`/`.bmp`/`.a` w gitignore)
- [x] (2026-09-11) Dynamiczne Menu Start (`FAT32::ListDirectory` + `VFS::ListDirectory`)
- [x] (2026-09-11) Ustawienia: zakładka Wyświetlacz, zakładka Dźwięk, System z żywym RAM
- [x] (2026-09-11) Fix realnego buga FAT32 (stary cache po zapisie w `SetFATEntry`)
- [x] (2026-09-11) Fix głębokiego buga schedulera — brak osobnego stosu jądra per-task, powodował niszczenie okien przy 2+ równoczesnych apkach Ring3
- [x] (2026-09-11) README.md z podziałem ról (kod — Claude, assety/testy — Kocurowy96)
